/**
 * It mounts the app on root
 * @author ayusharma
 */
import React from 'react';
import { render } from 'react-dom';
import Profile from './components/Profile';

render(<Profile />, document.getElementById('root'));
